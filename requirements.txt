# תלויות הרצה. גרסאות מקובעות לבנייה שחזורית.
#
# למה == ולא >=
# --------------
# עד 13.09 כל השורות היו >=, כלומר שתי התקנות באותו יום יכלו
# לקבל גרסאות שונות. בפרויקט שמטפל במידע רפואי ומשפטי, "עבד
# אצלי" אינו תשובה. הגרסאות כאן הן מה שנבדק בפועל.
#
# שדרוג הוא החלטה מודעת: לשנות כאן, להריץ את הבדיקות, ולקמט.

anthropic==1.4.0
fastapi==0.141.1
uvicorn[standard]==0.52.4

# העלאת קבצים (multipart/form-data)
python-multipart==0.0.32

# מסד נתונים
psycopg[binary]==3.3.5
bcrypt==5.0.0
cryptography==50.0.1
